-- use placement;

-- create table teacher
-- (
-- id int not null unique,
-- name varchar(100) not null,
-- email varchar(100) not null unique,
-- age tinyint check(age>=18),
-- status boolean default 1
-- );

-- insert into teacher
-- (id,name,email,age)
-- values
-- (2, "Verma", "verma33@example.com", 20);